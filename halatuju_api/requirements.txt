# Django and REST
Django>=5.0,<6.0
djangorestframework>=3.14,<4.0
django-cors-headers>=4.3,<5.0

# Database
psycopg2-binary>=2.9,<3.0
dj-database-url>=2.1,<3.0

# JWT verification (for Supabase Auth)
PyJWT>=2.8,<3.0

# Data processing (hybrid engine approach)
pandas>=2.0,<3.0
numpy>=1.24,<3.0

# Production server
gunicorn>=21.0,<23.0
whitenoise>=6.6,<7.0

# Observability
sentry-sdk>=1.39,<3.0

# AI (for reports)
google-genai>=1.0,<2.0
openai>=1.0,<3.0
# Cloud Vision OCR for the IC upload (S13 — soft signal, never blocks)
google-cloud-vision>=3.0,<4.0
# Off-platform backup of the private document bucket → GCS (security item A). Shares the
# google-auth / google-api-core deps already pulled in by google-cloud-vision (low marginal cost).
google-cloud-storage>=2.10,<4.0
# Google Calendar API client — auto-generates a Google Meet link + calendar event for a booked
# interview (apps/scholarship/meeting.py), via a Workspace service account with domain-wide
# delegation. google-auth is already present (pulled by google-cloud-*); this adds the API client.
# Optional at runtime: if absent or INTERVIEW_MEET_ENABLED is off, booking proceeds with no link.
google-api-python-client>=2.100,<3.0
google-auth-httplib2>=0.2,<1.0
# PDF document intake (scan-to-PDF + digital PDFs): read the text layer (pypdf)
# and rasterise page 1 for Vision (pypdfium2 + Pillow). Permissive licences;
# optional at runtime (a PDF degrades to "unreadable" if absent).
pypdf>=4.0,<6.0
pypdfium2>=4.0,<5.0
Pillow>=10.0,<12.0
# HEIF/HEIC opener for Pillow — iPhone photos upload as image/heic, which no browser can render
# and Cloud Vision can't OCR; we convert them to JPEG server-side on upload. Optional at runtime
# (a HEIC degrades to "open original" in the viewer if absent).
pillow-heif>=0.16,<1.0

# Development
python-dotenv>=1.0,<2.0
