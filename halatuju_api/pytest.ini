[pytest]
DJANGO_SETTINGS_MODULE = halatuju.settings.development
pythonpath = .
# A typo in a marker name is a test that silently never runs. The suite uses only
# django_db and parametrize, both registered by pytest/pytest-django.
addopts = --strict-markers
